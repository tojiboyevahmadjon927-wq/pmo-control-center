name: Deploy (local)

on:
  push:
    branches: [ main ]

env:
  DOCKER_BUILDKIT: 0
  COMPOSE_DOCKER_CLI_BUILD: 0

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        working-directory: C:\Users\User\OneDrive\Desktop\сайт
        run: git pull origin main

      - name: Restart Docker containers
        working-directory: C:\Users\User\OneDrive\Desktop\сайт
        run: docker compose up -d --build

      - name: Done
        run: echo Deployed!
